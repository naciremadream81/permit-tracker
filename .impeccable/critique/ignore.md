# Findings to ignore in future critiques

- `single-font` (browser detector): one family (Inter) is the documented doctrine for this product register — see DESIGN.md Typography.
- `flat-type-hierarchy` (browser detector): the tight ~1.125 fixed rem scale is intentional product-register typography — see DESIGN.md.
